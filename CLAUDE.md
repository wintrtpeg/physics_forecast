# CLAUDE.md

pforecast — 물리 지배방정식 기반 모델링·예측 툴. 디스플레이 팹 인프라 유틸리티 계통용.

## 실행

```bash
pip install -e ".[plot,dev]"
export PYTHONPATH=src            # 설치 없이 쓸 때
python -m pytest tests -q        # 전체 테스트 (수 초)
python -m pforecast.cli check examples/nox_stack/model.py
```

## 계층 규칙 (반드시 지킬 것)

```
L6 app/            <- 로컬 웹 앱 (표준 라이브러리만. 프레임워크·CDN 금지)
L5 cli.py / workflow.py
L4 scenario/ report/ selection.py analyze/
L3 data/ calib/
L2 lib/            <- 도메인(배기/열/유체) 지식은 여기까지만
L1 core/system.py core/structural.py core/solvers.py
L0 core/symbolic.py core/units.py core/component.py core/parser.py
```

* **`core/` 는 도메인을 몰라야 한다.** NOx, 스크러버, 공기 같은 단어가 들어가면 잘못된 것이다.
  `core/parser.py` 도 순수 수학 함수만 안다. 물성 함수(`density`, `enthalpy` ...)는
  `lib/generic.py` 의 `domain_functions()` 가 `FunctionTable` 에 얹는다.
* 새 계통(냉동기, CDA, 외조기)은 `lib/` 에 컴포넌트를 추가하는 것으로 끝나야 한다.
  `core/` 를 고쳐야 한다면 추상화가 부족한 것이니 먼저 그 점을 논의할 것.

## 컴포넌트 작성 규약

자세한 내용은 `docs/adding_components.md`. 핵심만:

* 방정식은 **잔차 형태**로 반환한다 (모두 0 이 되도록).
* `through` 변수의 부호는 **컴포넌트로 들어가는 방향이 +**. 출구 포트의 `mdot` 은 음수다.
  (이 규약을 헷갈려서 굴뚝 유량이 음수가 되는 버그를 한 번 냈다.)
* 방정식 개수: `포트수 x 3 + 내부변수수`. 경계 컴포넌트만 이 규칙에서 벗어난다.
* 차원이 있는 상수는 `S.const(9.80665, ACCEL)` 처럼 차원을 명시한다. 맨 숫자는 무차원이다.
* `abs()` / `x**0.5` 대신 `S.smooth_abs()` / `S.signed_pow()` 를 쓴다. 뉴턴법이 깨진다.
* `VarSpec` 의 경계는 "물리적으로 불가능한 영역"만 막는다. 참 해가 경계에 걸리면 수렴에
  실패한다 (건조 산소 몰분율 상한을 0.209 로 걸었다가 실제 값 0.2153 에서 실패한 적 있다).

## 수치 관련 주의

* `solve_steady` 는 BLT 블록 단위로 푼다. 수렴 실패 메시지에 어느 블록의 어느 변수/방정식인지 나온다.
* 수렴 판정은 절대값이 아니라 **행/열 스케일 대비 상대값**이다. 압력(1e5)과 질량분율(1e-4)이
  한 블록에 섞이기 때문.
* 야코비안은 희소 구조만 미분한다. dense 로 되돌리지 말 것 (코드 생성이 15배 느려진다).
* 심볼릭 스마트 생성자에서 **차원이 있는 상수 1.0 은 약분하면 안 된다**
  (`_dimensionless_const` 참고). 약분하면 차원 검사가 무력화된다.

## 데이터/보안

* 현장 CSV 는 커밋 금지 (`.gitignore` 에 차단되어 있다). 예제 합성 데이터 하나만 예외.
* 사내 태그 이름은 `tagmap.yaml` 에만 존재한다. **모델 코드에 태그를 넣지 말 것.**
* 보정된 파라미터는 코드가 아니라 `params_fitted.yaml` 로 관리한다.

## 선언형 컴포넌트 / 후보 비교

* `equations` 는 **dict** 다 (리스트 아님). 이름이 있어야 진단 메시지가 `SCR.closure_eta`
  처럼 뜨고, `extends` 로 **한 줄만 교체**할 수 있다.
* 차원이 있는 상수는 `273.15[K]`, `4186[J/(kg*K)]` 처럼 단위를 단다. 맨 숫자는 무차원이다.
* **후보로 둘 수 있는 것은 구성방정식(closure)뿐이다.** 보존법칙과 상태방정식을 후보로
  돌리면 외삽 보증이 사라진다 — 그러면 물리모델을 쓸 이유가 없다.
* 후보 비교에는 **반드시 하한선 모델**(가장 단순한 형태)을 넣는다. 그것보다 나은지
  확인하지 않으면 복잡도가 값을 하는지 알 수 없다.
* 선택 기준은 **외삽 구간 오차**가 1차다. 학습 적합도와 AICc 는 보조 지표다.
  무작위 k-fold 는 쓰지 말 것 — 학습/검증이 같은 분포가 되어 외삽 능력을 못 잰다.
* 후보 비교의 식별성 진단은 **그 후보에만 있는 파라미터** 기준으로 본다. 공통
  파라미터의 식별성은 설정의 성질이지 후보의 성질이 아니다.
* 시계열 잔차는 강하게 자기상관되어 있다. 후보 간 차이의 유의성은 **1차 자기상관으로
  유효 표본수를 깎아서** 판정한다 (`SelectionResult.paired_compare`).

## 검증 설계 — 미래·외삽·무누수 (위반하면 비교 결과를 보고하지 말 것)

* 물리 vs ML 비교 검증은 **미래이면서 외삽**이어야 한다. 학습 범위 안에서는 ML 도 잘
  맞히므로 차이가 안 드러난다. `data/split.check_split` 이 매번 두 가지를 검사해 보고한다.
* **값 기준 분할 금지** (예: 가동율 ≤ 0.55 학습 / ≥ 0.62 검증). 시간이 섞여서 검증의 88% 가
  학습보다 과거였던 적이 있다. 시간순으로 자르고 **엠바고 1일 이상**.
* 채점은 **학습 운전영역 밖 행**으로 한다. 안쪽 행은 따로 적는다.
* 교차검증은 **전진(forward-chaining)** 이다. 미래 블록으로 학습해 과거를 맞히지 않는다.
* 지연(lag)은 **0 이상만**. 피처의 미래 값으로 현재를 설명하면 누수다.
* 미래 예측 피처는 **drivers(미리 아는 값: 생산계획·예보·설정값)만**. 같은 시각의 결과값
  (굴뚝 유량·압력)을 쓰면 '현재값 추정'이다 — 그렇게 이름 붙여 따로 보고한다.
* 값 정제 통계는 **분할별로 따로** (`quality.assess_parts`). 검증 구간을 망가뜨려도 학습 쪽
  정제 결과가 한 비트도 안 바뀌어야 한다 (`tests/test_leakage.py`).
* 후보 선택은 `split.validate` 로, 최종 성능은 `test` 로 — test 로 고르고 test 로 보고하면
  선택 누수다.
* 탐지기 임계값을 조정한 데이터로 탐지 성능을 보고하지 말 것. `make_field_data.py
  --holdout` 으로 만든, 한 번도 안 본 변형으로 다시 잰다.
* 비교할 ML 에는 **트리 앙상블(부스팅)** 을 꼭 넣는다. 현업이 가장 많이 쓰고, 학습 범위
  밖에서 예측이 평평해진다. 다항식만 넣으면 ML 에 유리한 비교가 된다.

## 물리모델 개선 루프 (`pf improve`, `calib/design.py`, `docs/improvement_log.md`)

* 보정 행은 **1시간 평균 + 입력공간 최원점 30% + 시간 균등 70%** (`calibrate.sampling:
  space_filling`, 기본값). 드문 운전상태(펌프 정지·PM)가 발생량과 제거효율을 분리한다.
  5분 행 시간 간격(`stride`)은 그 행들을 놓친다.
* **좁은 기간으로 다시 맞춰 예측하지 말 것.** 학습 끝 3주 재보정은 내부 폴드에서 오차를
  절반으로 줄였지만 증설 후 예측을 세 배로 키웠다 (운전 다양성이 없어 귀속이 바뀌고, 월말
  분석계 드리프트와 오염 상태까지 흡수한다). NOx 만 체제 이후로 맞춰도 같았다.
* 짧은 내부 폴드는 **학습에서 한 번도 변하지 않은 방향**(증설 전 장비 대수)을 검증하지
  못한다. 그래서 내부 폴드 점수만으로 설계를 바꾸지 않는다 (복잡한 쪽은 2% 이상 나아야).
* 개선 판단은 **데이터 여러 벌**로 한다. 두 벌에서 좋아 보이던 설정이 다섯 벌에서는 아니었다.
  최종 확인용 데이터는 설계를 확정할 때까지 열지 않는다.
* 편향이 작으면 **상쇄**를 의심한다 (계측 드리프트 +1.8 과 체제 평균의 과소예측이 상쇄).
* 파라미터 시변성은 예측에 쓰지 않고 **진단**으로 보고한다. 긴 지평 예측의 구간은 파라미터
  공분산이 아니라 **학습 기간의 상태 변동 폭**(`state_band`)으로 말한다.
* 잔차 변화점과 날짜가 맞는 원인이 여럿이면 **전부** 적는다 (7/1 계단을 NTU 지수 탓으로
  잘못 적은 적이 있다 — 실제로는 같은 날의 월간 교정이었다).

## 데이터 주도 분석 (analyze/)

* **토폴로지는 데이터에서 유도할 수 없다.** L4 는 언제나 "막힘"으로 보고하고 무엇이
  필요한지 알려준다. 자동으로 계통 구조를 추정하려 들지 말 것.
* 영향인자는 **반드시 상관 묶음 기준**으로 먼저 말한다. 개별 순열 중요도는 상관된
  변수 사이에서 기여를 임의로 나눠 갖는다 (실제로 기여 60% 인 인자가 4위로, 7% 인
  인자가 1위로 나온 적이 있다).
* 교차검증은 **전진 시간 블록**(앞 블록으로만 학습)으로 한다. 무작위 k-fold 는 5분 데이터에서 앞뒤가 새어
  들어가 성능을 크게 과대평가한다.
* 보존식 탐지는 **작은 정수 계수를 열거**한다. SVD 최소특이벡터는 공선적 데이터에서
  노이즈를 따라간다 (±1 이어야 할 계수가 4,4,3,−3,−3 으로 나왔다).
* 개선 제안은 **학습 포락선 밖으로 나가지 않는다.** 제어변수의 영향력이 5% 미만이면
  방향이 뒤집힐 수 있다고 경고한다.
* pandas 의 `.to_numpy()` 는 읽기전용 뷰를 줄 수 있다. 쓰기 전에 `np.array(...)` 로 복사할 것.

## 앱 (app/)

* **의존성을 늘리지 말 것.** 표준 라이브러리 + 기존 패키지만. 프런트엔드는 CDN 금지
  (사내 PC 는 외부 요청이 막혀 있다고 가정한다).
* 짧은 작업(풀이·프로파일링)은 요청 안에서, 긴 작업(분석·보정)은 스레드 + 폴링.
* 운전 손잡이는 모델이 `DRIVERS` 로 선언하는 것이 우선이다. 자동 추출은 대기압·
  기준밀도 같은 상수까지 끌어온다.
* 손잡이 값은 **선언한 단위**로 환산한다. 파라미터 단위로 환산하면 "가동율 100%" 가
  util=100 이 되어 버린다 (실제로 낸 버그).
* 모델을 바꾸면 KPI·관리기준·스윕 선택을 **전부 초기화**한다. 남기면 이전 계통의
  기준이 엉뚱하게 따라붙는다.
* `h()` 헬퍼에 `{html:...}` 를 자식 자리로 넘기지 말 것 (속성 자리다). 지금은
  TypeError 를 던지지만 원래는 `[object Object]` 가 화면에 찍혔다.
* YAML 1.1 은 `3.5e6` 을 **문자열**로 읽는다 (`3.5e+6` 이라야 실수). 선언형 컴포넌트는
  `_maybe_float` 로 방어한다.

## 현장 데이터 수집 (data/ingest.py, data/quality.py)

* **CSV 를 `pd.read_csv` 로 직접 읽지 말 것.** `read_table()` 을 쓴다. 한글 엑셀 저장본은
  CP949 이고, 태그/설명/단위 3행 헤더·`2025-01-01 오후 3:05` 날짜·`Bad`/`Comm Fail` 같은
  상태 문자열·천 단위 콤마가 섞인다. `read_csv` + `select_dtypes(number)` 는 이걸
  **에러 없이 전부 버린다** (실제로 26개 컬럼 중 25개, 석 달 치 행이 조용히 사라졌다).
* 고친 것은 전부 `IngestReport.lines()` 로 보고한다. 조용히 고치지 않는다.
* 수집 결과는 **규칙 격자**(빠진 시각은 NaN 행)다. 지연·이동창·연속 구간 판정이
  '행 수 = 시간'이어야 맞는다.
* 값 이상은 결측으로만 바꾼다. **보간·대체로 값을 지어내지 말 것.**
* 값이 정확히 0 에 머무는 구간(펌프 정지)은 고착이 아니다. 지우면 모델이 배워야 할
  공정 이벤트를 지우는 것이다.
* 스파이크 잡음 추정의 하한은 **분해능**이다. 0.01 단위 반올림 신호는 MAD 가 0 이 되어
  0.01 변화도 스파이크로 잡힌다 (예제 인버터 주파수에서 실제로 났다).
* 정기 교정 창은 **먼저 빼고 스파이크를 다시 본다.** 창 옆 행이 비면 이동 중앙값이
  교정값에 끌려가 멀쩡한 이웃 점이 잡힌다.
* 날짜 비교에 `np.isin(DatetimeIndex, [Timestamp...])` 을 쓰지 말 것. 조용히 전부
  불일치한다. 정수 일 번호로 바꿔 비교한다.
* 원시 신호의 계단 변화를 이상으로 판정하지 않는다. 증설·설정 변경도 계단이다.
  그 구분은 물리 잔차가 한다 (`calib/diagnostics.py`).
* 보존식은 **이득 보정 후** 판정한다. 계측기 하나가 3% 높게 읽으면 보존식인데도 R² 가
  망가진다. 그 이득 자체가 계측기 진단이다. degC 처럼 영점이 임의인 단위는 이득 대신
  **영점 차이**로 말한다.
* 학습 구간에서 변하지 않은 피처(증설 전 장비 대수)는 대리모델에서 빼고 **반드시 알린다.**
  ML 이 원리적으로 못 보는 변화가 정확히 이것이다.
* 현장형 더미 데이터(`examples/nox_field/`)의 정답지는 `data/_answer/` 에 둔다. 앱은
  `_` 로 시작하는 폴더를 목록에서 뺀다.

## 캘리브레이션 원칙

* 보정 대상은 **물리 파라미터**만 (`ParamSpec(tunable=True)`). 회귀계수를 만들지 말 것.
* 결과에는 항상 식별성 진단을 같이 낸다. 공분산은 **정칙화 항을 뺀 데이터 항만으로** 계산한다.
  (정칙화를 포함하면 상관이 인위적으로 낮아져 "식별된 것처럼" 보인다.)
* 식별 안 되는 파라미터는 설계값에 고정하고 나머지만 보정한다.
* 경계에 붙었는지는 **최적화가 실제로 쓴 경계**(`CalibrationResult.at_bound()`) 로 본다.
  모델이 선언한 넓은 물리 경계로 보면 작은 양수 파라미터가 늘 오진된다.
* 입력은 파라미터의 물리 범위로 **잘라서** 넣는다 (`build_param_rows(clip=True)`).
  순환수 0 은 `L/G^b` 의 기울기를 발산시켜 뉴턴법을 죽인다. 경계(1e-6)면 '효율 0' 이
  올바르게 나온다.
* 관측은 하나라도 있으면 그 행을 쓴다. 비어 있는 관측 칸은 잔차 0 (벌점 아님).
  설계값에서도 안 풀리는 행은 벌점 대신 **빼고 개수를 보고**한다.

## 문서 작성 시

README 와 `docs/` 의 수치는 **실제로 실행해서 얻은 값만** 적는다. 추정치나 기대값을
쓰지 말 것.
